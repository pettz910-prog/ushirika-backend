package com.mdau.ushirika.module.attendance.enums;

public enum AttendanceStatus {
    PRESENT,
    LATE,
    ABSENT,
    EXCUSED  // Excused absence — does not count toward the consecutive termination rule
}
